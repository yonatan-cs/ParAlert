# Copy to .env (or export). All optional — defaults shown.
BACKEND_URL=http://localhost:8000/ingest
GROUPS_ONLY=true
# Explicit browser path; leave unset to auto-detect system Chrome/Edge.
# CHROME_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe
